declare namespace Sup {
    namespace Audio {
        class MultiSoundPlayer {
            constructor(init: string|Sound, loop: string|Sound, tails: Array<any>, volume: number /* 1.0 */, options?: { loop?: boolean; pitch?: number; pan?: number; });
            play(beatNum: number /* 0 */);
        }
    }
}